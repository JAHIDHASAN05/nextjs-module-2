import React from 'react';

export const metadata={
    title:"about",
    description:"this is about page description"
}

const AboutPage = () => {
    return (
        <div>
            <h1>this is baout page</h1>
        </div>
    );
};

export default AboutPage;